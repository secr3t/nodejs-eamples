<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
    <h1>사용자 리스트</h1>
    <p><%=type%></p>
    <table border="1" width="100%">
        <thead>
        <tr>
            <th>사원번호</th>
            <th>사원명</th>
            <th>소속부서</th>
        </tr>
        </thead>
        <tbody>
        <% if(employees.length) {%>
        <% employees.forEach(function(emp, index) { %>
        <tr>
            <td><%=emp.no%></td>
            <td><%=emp.name%></td>
            <td><%=emp.dept%></td>
        </tr>
        <% });%>

        <% } else {%>
        <tr>
            <td colspan="3">검색된 결과가 없습니다.</td>
        </tr>
        <% } %>
        </tbody>
    </table>
</body>
</html>